# Kibana Dashboards Downloader

# Installation

Run `npm install` and edit `config.json` which you can copy from `config.json.example`.

# Usage

Just run `./kdd` and the dashboards will save to the specify directory.
